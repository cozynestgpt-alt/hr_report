# 작업이력

월례회의 인원보고서 자동화(부서별인원현황) 프로젝트의 코드·문서·구조 변경 이력입니다.
매월 input 파일 교체 같은 통상 운영 작업은 기록하지 않고, 스크립트/문서/폴더 구조에 실질적인 변경이 있을 때만 추가합니다.

## 2026-08-11

- **폴더명 변경 반영** — 로컬 PC·NAS 폴더가 `hr_report` → `부서별인원현황`으로 바뀜에 따라 CLAUDE.md·README·NAS 매뉴얼·bat의 경로 문구를 갱신 (GitHub 저장소명은 `hr_report`로 유지, `git remote` 확인 완료) `4ef2bf3`
- **입력 파일 형식 전환** — `사원명단`/`사원현황` 파일 대신 `부서별인원현황_YYYYMMDD.xlsx`(부서코드별 집계표)를 입력으로 쓰도록 `main.py` 전면 재작성. 구 부서코드 매핑(`OLD_CODE_MAP`), 직영점·위탁매장 자동 집계 로직 추가 `4ef2bf3`
- **output 버전 관리** — 같은 월을 재실행해도 기존 결과 파일을 덮어쓰지 않고 `_v2`, `_v3`...로 새로 저장 `4ef2bf3`
- **중간관리·물류센터용역 자동 반영** — 담당자가 매월 직접 갱신하는 `input/★월례회의_인원보고_양식.xlsx`의 L2(기준일)가 당월 말일과 일치할 때만 34~37행(중간관리)·43~44행(물류센터용역·평택점용역)을 최종 보고서에 자동 복사. 불일치하거나 파일이 없으면 조용히 넘어가지 않고 콘솔 경고 후 수동 입력으로 안내 `4ef2bf3`
  - 계기: `template/월례회의_인원보고_템플릿.xlsx`의 해당 행에 과거(6월) 수기입력 값이 그대로 박혀있어, 매달 재사용 시 옛 숫자가 무단 반복될 위험을 발견 → 담당자가 관리하는 소스 파일에서 기준일 검증 후 가져오는 방식으로 해결
- **인원수 파싱 견고화** — `"12.0"` 같은 소수점 문자열도 정상 처리하도록 수정. 그래도 파싱이 안 되는 값은 조용히 0으로 처리하지 않고 `부서코드=... 값=...` 형태로 콘솔 경고 출력 `4ef2bf3`
- **Windows 크래시 수정** — `scripts/office/soffice.py`가 `AF_UNIX` 미지원 환경(Windows)에서 `AttributeError`로 죽던 문제 수정 `4ef2bf3`
- **옛 input 파일 정리** — 더 이상 쓰이지 않는 `사원명단(...).xlsx`·`사원현황(...).xlsx` 12개 파일 삭제 (`.gitignore` 대상이라 git 이력 없음, 백업 없이 영구 삭제)
- **bat 오류 처리 버그 수정** — `main.py`가 실패해도 "작업이 완료되었습니다"가 뜨던 문제 수정 (`errorlevel` 확인 후 오류 화면 표시). 파일이 LF 줄바꿈으로 저장돼 있어 cmd.exe가 괄호 블록을 오파싱하던 문제를 CRLF 정규화 + goto 기반 오류 분기로 해결 `7ff1601`
  - 확인 필요: 실행 시 배너에 "The system cannot find the path specified." 한 줄이 섞여 나오는 현상이 남아있음(기능에는 영향 없음으로 보임) — 원인 추적 중

## 2026-07-13

- 프로젝트 초기 구성 — 부서별 인원 현황 자동 집계 스크립트, 템플릿, 폴더 구조(`.gitkeep`) 최초 커밋 `718d818`
- README에 GitHub 저장소 주소 추가 `2bd5c69`
- 팀원용 NAS 실행환경 구성 — `월례회의_인원보고_실행.bat`, `CLAUDE.md`, `NAS_팀원_실행_매뉴얼.md` 추가 `e33107a`
